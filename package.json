{
  "name": "api-rest-enviame",
  "version": "1.0.0",
  "description": "",
  "main": "dist/app.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build && npx prisma generate && npx prisma db push --accept-data-loss --preview-feature && npx prisma db seed --preview-feature",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prisma-generate": "npx prisma generate",
    "prisma-seed": "npx prisma db seed --preview-feature",
    "prisma-push": "npx prisma db push --accept-data-loss --preview-feature"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^2.17.0",
    "celebrate": "^14.0.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "method-override": "^3.0.0",
    "mysql": "^2.18.1",
    "reflect-metadata": "^0.1.13",
    "typedi": "^0.10.0",
    "winston": "^3.3.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.10",
    "@types/express": "^4.17.11",
    "@types/node": "^14.14.37",
    "cors": "^2.8.5",
    "prisma": "^2.20.1",
    "tslint": "^6.1.3",
    "typescript": "^4.2.3"
  }
}
